{% extends 'base.html.twig' %}
{% block body %}
    <h6 class="border-bottom border-gray pb-2 mb-0">Public Lists</h6>
    {% for param in params %}
        <a href="{{ path('public_list_tasks', { 'id':param.id }) }}">
            {{ include('tasks/row-single-list-list.html.twig', { 'tasks':param }) }}
        </a>
        <small class="d-block text-left mt-3 border-bottom border-gray">
        </small>
    {% endfor %}
{% endblock %}
